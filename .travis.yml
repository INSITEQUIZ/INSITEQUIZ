language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules #Cache node_modules for faster builds
script:
  - npm install
  - npm run start

after_script:
  - echo "BUILD FINISHED"
  
